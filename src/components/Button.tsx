const buttonVariants = {
	default:
		'inline-flex flex-row items-center justify-center bg-brand text-lg w-full text-white disabled:bg-brand/80 h-10 px-4 rounded-md',
	outline:
		'inline-flex flex-row items-center justify-center h-10 px-4 rounded-md border-brand border-2',
};

const buttonTextVariants = {
	default: 'text-white text-xl text-center',
	outline: 'text-brand text-xl text-center',
};

export {buttonTextVariants, buttonVariants};
