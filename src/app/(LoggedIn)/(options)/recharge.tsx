import {View} from 'react-native';
import AccountHeader from 'src/components/AccountHeader';

export default function Recharge() {
	return (
		<View>
			<AccountHeader title='Recharge' />
		</View>
	);
}
