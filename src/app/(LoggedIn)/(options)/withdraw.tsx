import {View} from 'react-native';
import AccountHeader from 'src/components/AccountHeader';

export default function Withdraw() {
	return (
		<View>
			<AccountHeader title='Withdraw' />
		</View>
	);
}
